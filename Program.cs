using Truco.Entities;
using Truco.Entities.Enums;

namespace Truco;
class Program {
    static void Main(string[] args) {
        // Random rand = new Random();
        // Carta c1 = new Carta((Valor)rand.Next(1, 14));
        // Console.WriteLine(c1);

        // List<Carta> cartas = new List<Carta>();
        // for (int i = 0; i<=40; i++) {
        //     cartas.Add(new Carta((Valor)rand.Next(1, 14)));
        // }

        // foreach (Carta c in cartas) {
        //     Console.WriteLine(c);
        // }


        // string teste = "Willian Eduardo Soares da Silva";

        // bool contem = teste.Contains("Willian");

        // Console.WriteLine(contem);


        Jogador j1 = new Jogador();

        Console.WriteLine(j1);

        // Carta c1 = new Carta((Valor)1);
        // Carta c2 = new Carta((Valor)2);
        // Carta c3 = new Carta((Valor)1);

        // Console.WriteLine(c1.Equals(c3));
    }
}